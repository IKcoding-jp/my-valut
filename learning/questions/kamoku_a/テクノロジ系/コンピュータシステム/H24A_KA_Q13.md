---
id: H24A_KA_Q13
year: H24秋
kamoku: A
number: 13
type: 暗記
field: テクノロジ系 » システム構成要素 »システムの構成
difficulty: 3
status: 未学習
source: "出典：平成24年秋期 基本情報技術者試験 科目A 問13"
source_url: "https://www.fe-siken.com/kakomon/24_aki/q13.html"
last_attempt: null
confidence_history: []
result_history: []
time_spent_seconds: []
---

## 問題
3層クライアントサーバシステム構成で実現したWebシステムの特徴として，適切なものはどれか。

## 選択肢
- ア: HTMLで記述されたプログラムをサーバ側で動作させ，クライアントソフトはその結果を画面に表示する。
- イ: 業務処理の変更のたびに，Webシステムを動作させるための業務処理用アプリケーションを配布し，クライアント端末にインストールする必要がある。
- ウ: 業務処理はサーバ側で実行し，クライアントソフトはHTMLの記述に従って，その結果を画面に表示する。
- エ: クライアント端末には，サーバ側からのHTTP要求を待ち受けるサービスを常駐させておく必要がある。

## 正解
ウ

## 解説
クライアントサーバシステム
(client-server system:C/S)はサービスを要求する側であるクライアントと、サービスを提供するサーバに分離したコンピュータネットワークの形態です。
3層クライアントサーバシステム
では、ユーザーの入出力を担当するプレゼンテーション層，業務処理に依存するデータ加工を行うファンクション層，データベース処理を行うデータ層の3層に分離したモデルです。
一般にWeb3層クライアントシステムは、
の組合せで構成され、それぞれの層に伝達しながら処理が行われます。
上記を踏まえて選択肢の記述が正しいかを検証します。

### 各選択肢の解説
プレゼンテーション層＝Webブラウザ
ファンクション層＝Webサーバ
データ層＝DBMSとデータベース
ブラウザは利用者の入力をサーバに送信する(HTTP要求)。
WebサーバではHTTP要求を処理し、データの読み書きをデータベース(DBMS)に要求する。
DBMSはデータベースにアクセスし、結果をWebサーバに返す。
WebサーバではDBMSから得られた結果に応じてHTMLページを構成し、付加情報とともにクライアントに返信する(HTTP応答)。
ブラウザはWebサーバからのHTTP応答を解釈し、結果を画面に出力する。
- ア: HTML(HyperText Markup Language)は、Webページを記述するマークアップ言語でサーバ上で動作するものではありません。
- イ: WebシステムではクライアントソフトとしてWebブラウザを使用するので、専用のアプリケーションのインストールなどは必要ありません。
- ウ: 正しい。
- エ: HTTP要求はクライアント側からサーバへ行われます。WebサーバではHTTPサーバプログラムを常駐させておく必要があります。

### 関連概念
- [[データベース]]
- [[ネットワーク]]
